# version: '1'

api:
  dashboard: true
  debug: true
entryPoints:
  http:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: https
          scheme: https
  https:
    address: ":443"

#serversTransport:
providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
  file:
    filename: /config.yml
certificatesResolvers:
  myresolver:
    acme:
      email: mcc@kronos.cloudns.ph
      storage: acme.json
      # DNS challenge con Cloudflare: no depende de acceso entrante por puerto 80.
      dnsChallenge:
        provider: cloudflare
        # Reduce errores por propagacion DNS lenta.
        delayBeforeCheck: 30
